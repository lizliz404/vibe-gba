{
  "scripts": {
    "build:wasm": "wasm-pack build --target web --release --out-dir site/play/pkg",
    "check": "cargo fmt --check && cargo test && cargo check --lib --target wasm32-unknown-unknown && npm run build:wasm && node --test tests/site.test.mjs",
    "test:site": "node --test tests/site.test.mjs"
  }
}
